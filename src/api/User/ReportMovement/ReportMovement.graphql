type ReportMovementResponse {
    ok: Boolean!,
    error: String
}

type Mutation {
    ReportMovement(
        orientation: Float, 
        lastLat: Float, 
        lastLag: Float
    ): ReportMovementResponse!
}
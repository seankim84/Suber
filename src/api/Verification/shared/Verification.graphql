type Verification { #Verification 이기 때문에 모든 값은 필수로 지정한다.
    id: Int!
    target: String!
    payload: String! # like a key   
    key: String!
    verified: Boolean!
    createdAt: String!
    updatedAt: String!
}
